@page "/game2"
@using KingSkullClassicOnline.Client.Shared.Components

@inject IDialogService DialogService

<PageTitle>Games</PageTitle>

<GameBoard IsVisible="true" Cards="_cards"/>

@code {


    private readonly List<string> _cards = new()
    {
        "Red_2", "Blue_1", "Pirate_1", "ScaryMary", "Escape", "Red_2", "Blue_1", "Pirate_1", "ScaryMary", "Escape"
    };

    // Pop-up des scores
    private void OpenScoresDialog()
    {
        var options = new DialogOptions { CloseOnEscapeKey = true };
    // DialogService.Show<ScorePopup>("Tableau des scores", options);
    }

}